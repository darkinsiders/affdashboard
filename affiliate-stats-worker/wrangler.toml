name = "affiliate-stats-fetcher"
main = "worker.js"
compatibility_date = "2024-01-01"
workers_dev = true

# Cron disabled — PebbleHost Python script handles all brands now
# The /run-stats endpoint still works for manual testing
# [triggers]
# crons = ["0 * * * *"]
